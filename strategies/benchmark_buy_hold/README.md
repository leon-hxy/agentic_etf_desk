# Benchmark Buy Hold

Repo-only ETF research template for a simple buy-and-hold benchmark. It reads symbols only from `configs/universe/etf_universe.yaml` and compares every result against the configured benchmark.

This template produces research and backtest outputs only. This is research advice, not automatic order placement. 最终交易由用户手动决定。
